{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  {{- $seen := newScratch }}
  {{- range .Data.Pages }}
    {{- $isThinTaxonomy := and (or (eq .Kind "taxonomy") (eq .Kind "term")) (lt (len .Pages) 3) }}
    {{- $isUtilityPage := in (slice "/about/" "/search/" "/archives/") .RelPermalink }}
    {{- $alreadySeen := $seen.Get .Permalink }}
    {{- if and (not $isThinTaxonomy) (not $isUtilityPage) (not $alreadySeen) }}
      {{- $seen.Set .Permalink true }}
  <url>
    <loc>{{ .Permalink }}</loc>{{ with .Lastmod }}
    <lastmod>{{ .Format "2006-01-02T15:04:05-07:00" }}</lastmod>{{ end }}{{ with .Sitemap.ChangeFreq }}
    <changefreq>{{ . }}</changefreq>{{ end }}{{ if ge .Sitemap.Priority 0.0 }}
    <priority>{{ .Sitemap.Priority }}</priority>{{ end }}
  </url>
    {{- end }}
  {{- end }}
</urlset>
